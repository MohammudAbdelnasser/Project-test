name: project_2
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.30.0
  firebase_auth: ^4.17.9  # إذا كنت تستخدم Firebase Auth
  cloud_firestore: ^4.17.0
  firebase_storage: ^11.6.0
  provider: ^6.1.2
  image_picker: ^1.0.7


  cupertino_icons: ^1.0.8
  email_validator: ^3.0.0
  flutter_bluetooth_serial: ^0.4.0
  permission_handler: ^11.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/HomePage/
    - assets/PersonalCare/
